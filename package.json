{
  "name": "xdeporter",
  "version": "0.1.1",
  "private": true,
  "description": "Brave extension for X account-country labels and local feed filtering",
  "scripts": { "test": "node --test tests/*.test.cjs", "test:browser": "node tests/browser.cjs" },
  "engines": { "node": ">=20" }
}
